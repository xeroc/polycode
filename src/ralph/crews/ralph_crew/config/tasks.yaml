implement_task:
  description: |
    ## Single Iteration of Ralph Loop

    You are running **iteration {iteration}** of {max_iterations}.

    ### STORY TO IMPLEMENT:
    {story}

    ### TASK:
    {task}

    ### ENVIRONMENT:
    - REPO: {repo}
    - BRANCH: {branch}
    - BUILD_CMD: {build_cmd}

    ### PREVIOUS ERRORS (from earlier iterations):
    {previous_errors}

    ### AGENTS.md REFERENCE:
    {agents_md}

    ### AVAILABLE TOOLS:
    - FileReadTool: Read file contents
    - FileWriterTool: Write/modify files
    - DirectoryReadTool: List directory structure
    - ExecTool: Run shell commands (npm, etc.)

    ### INSTRUCTIONS:

    1. Implement → feature described in the story
    2. Keep changes minimal and focused
    3. Follow existing code patterns in the repo
    4. If previous_errors are provided, AVOID those specific mistakes

    5. Generate → a conventional commit message
    6. Report → what was changed
    7. Set status:
       - "done" if build passed
       - "retry" if build failed (so Flow can iterate)

    ### COMMIT MESSAGE FORMAT:
    - title: `<type>(<scope>): <subject>` (max 72 chars)
    - message: Body explaining what and why (wrapped at 72 chars)
    - footer: Breaking changes or issue refs (optional)

    Types: feat, fix, docs, style, refactor, perf, test, chore

  expected_output: |
    RalphOutput with:
    - status: "done" (build passed) or "retry" (build failed)
    - changes: What was implemented
    - title: Commit message title
    - message: Commit message body
    - footer: Commit footer (empty if none)
  agent: implementer
