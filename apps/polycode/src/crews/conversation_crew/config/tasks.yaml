spec_elicitation_task:
  description: |
    ## Requirements Clarification

    ### CURRENT CONTEXT:
    - Issue: {issue_id}
    - Tas: {task}
    - Conversation History:
    {% for c in conversation_history %}
    - {{c.author}}: {{c.body}}
    {% endfor %}

    REPO: {repo}
    BRANCH: {branch}

    FILES IN REPO/WORKING DIRECTORY (honors .gitignore!):
    ```
    {file_in_repos}
    ```

    AGENTS.md:
    ```
    {agents_md}
    ```

    TOOLS:
    - FileReadTool: Read the content of a file
    - AgentsMDLoaderTool: Load AGENTS.md files from subdirectories (use when needed)

    ### TASK:
    Based on the current conversation context, either:
    1. Ask a clarifying question if requirements are unclear
    2. Propose a final specification if requirements are clear

    ### GUIDELINES:
    - Ask ONE question at a time
    - Focus on edge cases, constraints, and acceptance criteria
    - When proposing a spec, summarize all requirements clearly
    - Use markdown formatting for readability
    - Ask human to thumbs up (👍) to approve the specification

    ### OUTPUT:
    If asking question: A clear, specific question
    If proposing spec: A complete specification document with:
    - Feature overview
    - Detailed requirements
    - Acceptance criteria
    - Assumptions (if any)
  expected_output: |
    Either:
    1. List of questions that help clarify uncertainties
    2. A complete document ready for approval, includin specifications, requirements and assumptions
    Pick an option, leave the unpicked option undefined in your structured response.
  agent: spec_elicitor
